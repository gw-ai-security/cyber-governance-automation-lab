# =============================================================
# .gitignore — cyber-governance-automation-lab
# Python / VS Code / Windows / Power Automate / Power BI project
# =============================================================

# -------------------------------------------------------------
# 1. Python
# -------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class

# Testing / coverage / type-checker caches
.pytest_cache/
.cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.pyre/
.pytype/
.ruff_cache/
.tox/
.nox/

# Build / distribution artifacts
build/
dist/
*.egg-info/
.eggs/
*.egg
wheels/
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/
ENV/

# -------------------------------------------------------------
# 2. Secrets & Environment files
# -------------------------------------------------------------
.env
.env.*
!.env.example

*.pem
*.key
*.p12
*.pfx
*credentials*.json
*secrets*.json
.secrets/

# -------------------------------------------------------------
# 3. VS Code / IDE (conservative)
# -------------------------------------------------------------
# Only ignore user-/machine-local VS Code state, keep shared
# workspace configuration (settings.json, extensions.json, etc.)
# versionable on purpose.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.history/
*.code-workspace

# Other common IDEs
.idea/

# -------------------------------------------------------------
# 4. Windows
# -------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# -------------------------------------------------------------
# 5. Logs & temporary files
# -------------------------------------------------------------
*.log
logs/
*.tmp
*.temp
*.bak
*.swp
~$*
.tmp/

# -------------------------------------------------------------
# 6. Power BI (only clearly local/user-specific artifacts)
# -------------------------------------------------------------
# Local Power BI Desktop cache/temp files — not the project's
# .pbip/.pbix/report/semantic-model/documentation files.
*.pbi.bak
**/.pbi/localSettings.json
**/.pbi/cache.abf
~*.pbix
*.pbix.lock

# -------------------------------------------------------------
# 7. Generated curated runtime outputs
# -------------------------------------------------------------
# The Python pipeline writes reproducible runtime artifacts here.
# Keep the directory itself, but do not commit generated snapshots.
data/curated/*
!data/curated/.gitkeep

# -------------------------------------------------------------
# 8. Operational Microsoft 365 workbook (Phases 5–6)
# -------------------------------------------------------------
# The live workbook can contain authenticated responder identity
# and reachable acceptance-test recipients. It is an operational
# PoC artifact, not a canonical repository dataset.
Cyber_Governance_Control_Register.xlsx

# -------------------------------------------------------------
# 9. Private Power Automate deployment/runtime artifacts
# -------------------------------------------------------------
# Unmanaged tenant exports can contain environment-specific
# resource identifiers and reachable notification recipients.
# Only sanitized source templates belong in the public repository.
power_automate/exports/
power_automate/private/

# Operational Phase 7 snapshot packages remain private.
power_automate/snapshots/

# -------------------------------------------------------------
# 10. Project-specific keep-list / do NOT ignore
# -------------------------------------------------------------
# Canonical source/reference data, documentation evidence, future
# phase directories, requirements, source code, tests, and the
# sanitized Power Automate source templates remain versionable.
# data/curated is the exception documented above: only its
# .gitkeep is retained until a later phase deliberately introduces
# a versioned artifact contract.
!data/raw/
!data/reference/
!docs/screenshots/
!ai/prompts/
!ai/schemas/
!ai/examples/
!power_automate/solutions/
!requirements.txt
!src/
!tests/
